services:
  - type: web
    name: stagify-ai
    env: node
    plan: starter
    buildCommand: npm install && npm test
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GOOGLE_AI_API_KEY
        sync: false
    autoDeploy: true
